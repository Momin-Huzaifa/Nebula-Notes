{
  "name": "mock-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 10000",
    "server": "npm start"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
